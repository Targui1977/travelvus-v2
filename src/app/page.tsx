import { QuickCompare } from "@/components/compare";

/**
 * Travelvus V2 — Quick Compare
 *
 * Phase 2 surface. The user brings two flights they already found.
 * Travelvus will judge which really wins.
 *
 * This page will evolve as we add Full Result, Verdict Changed,
 * and Decision Pages in subsequent phases.
 */
export default function ComparePage() {
  return <QuickCompare />;
}
